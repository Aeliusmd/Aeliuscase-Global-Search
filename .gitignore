# dependencies
node_modules/
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# env files — NEVER commit these
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

#Claude
.claude/
*.claude
CLAUDE.md

# Cursor — local IDE config and plans (never commit)
.cursor/
.cursor/plans/uat_chatbot_integration_14c8efde.plan.md
CURSOR.md

# Not required to run the app — kept local only, excluded from the repo
src/
tests/
detections/

# Keep the multi-tenant auth regression tests in the repository.
!tests/
tests/*
!tests/api/
tests/api/*
!tests/api/authVerify.test.ts
!tests/lib/
tests/lib/*
!tests/lib/authEnvelope.test.ts
!tests/lib/authSession.test.ts
!tests/lib/rateLimit.test.ts
!tests/helpers/
tests/helpers/*
!tests/helpers/inMemoryMongo.ts
!tests/middleware.test.ts

# Agent-generated planning docs — kept local only
docs/

# The UAT integration contract is a product deliverable, not a planning note.
!docs/
docs/*
!docs/uat-integration.md

# Documents — large files, kept local only
*.docx
*.doc
*.pdf

# Local screenshots and HTML exports — kept local only
*.png
*.html
